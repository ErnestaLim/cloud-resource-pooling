from setuptools import setup, find_packages

setup(
    name="llm_eval",
    version="0.1.0",
    description="Library to connect and retrieve LLM evaluation results",
    packages=find_packages(),
    install_requires=[],
)
